const mongoose = require( 'mongoose' );


/*
Homework{
    homeworkID,
    title,
    text,
    imageURL, (optional)
    HomeworkDelivery{
        studentID,
        HomeworkURL,
        grade
    }
}
*/ 